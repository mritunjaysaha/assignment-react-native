import {StyleSheet} from 'react-native';

export const CreateBusinessFormScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fff',
  },
  formContainer: {
    paddingHorizontal: 20,
    width: '100%',
    gap: 20,
  },
});
