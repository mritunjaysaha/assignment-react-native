import {StyleSheet, Text, View} from 'react-native';

export const CreateBusinessFormScreen = () => {
  return (
    <View style={styles.container}>
      <Text>Add Business Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fff',
  },
});
